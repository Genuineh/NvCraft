-- 模块模板
-- 复制此文件到 lua/core/modules/ 并根据需要修改

return {
    -- ============================================================================
    -- 插件信息
    -- ============================================================================
    
    -- 插件仓库地址（必需）
    "author/plugin-name",
    
    -- 版本控制（可选）
    version = "1.0.0",  -- 使用特定版本
    -- 或
    -- branch = "main",    -- 使用特定分支
    
    -- ============================================================================
    -- 依赖项（可选）
    -- ============================================================================
    
    dependencies = {
        -- 简单依赖
        "other/plugin",
        
        -- 带配置的依赖
        {
            "another/plugin",
            config = function()
                require("another.plugin").setup({
                    -- 依赖配置
                })
            end
        }
    },
    
    -- ============================================================================
    -- 懒加载配置（可选）
    -- ============================================================================
    
    -- 基于事件懒加载
    event = "VeryLazy",  -- 或 "BufReadPost", "InsertEnter" 等
    
    -- ============================================================================
    -- 插件配置
    -- ============================================================================
    
    -- 简单配置选项
    opts = {
        enable = true,
        option1 = "value1",
    },
    
    -- ============================================================================
    -- 快捷键配置（可选）
    -- ============================================================================
    
    keys = {
        {
            "<leader>xx",
            ":PluginCommand<CR>",
            desc = "执行插件命令"
        },
    },
    
    -- ============================================================================
    -- 初始化函数（可选）
    -- ============================================================================
    
    init = function()
        -- 在插件加载前执行
    end,
}
